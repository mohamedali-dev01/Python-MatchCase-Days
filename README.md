# Day Selector :

A Python-based utility that maps integer inputs to their corresponding days of the week. This project demonstrates the implementation of structural pattern matching introduced in Python 3.10.

# Description :

The script takes an integer input from the user (1-7) and returns the associated weekday. It utilizes the `match-case` statement to provide a cleaner and more efficient alternative to traditional conditional branching.

